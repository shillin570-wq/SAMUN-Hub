# SAMUN 本地离线会议系统

这是一个用于模联会议流程管理的本地前端工具，默认离线运行，不依赖联机服务。

## 功能流程

1. 会议设置
2. 代表点名
3. 会议进行（发言名单 + 计时）
4. 投票表决

支持本地存档（浏览器 `localStorage`），可保存和读取会议进度。

## 本地启动

**前置条件：** Node.js 18+

1. 安装依赖：`npm install`
2. 启动开发环境：`npm run dev`
3. 打开：`http://localhost:3000`
